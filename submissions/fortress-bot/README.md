# Fortress Bot Notes

Official competition servers currently run `llama3.1:8b`, so this submission defaults to that model in both code and metadata.

For local experiments, you can override with:

```bash
export OLLAMA_MODEL=qwen3.5:9b
```

The bot will use `OLLAMA_MODEL` when set.
